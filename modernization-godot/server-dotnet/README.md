# Server (dotnet)

This is a minimal .NET 8 headless server scaffold.

Run
```
dotnet run
```

Next steps
- Replace the TCP placeholder with a real packet protocol.
- Add a fixed tick simulation loop with entity storage.
- Load content data from converted JSON.
