# Tools

This folder will contain utilities for:
- Parsing legacy VB6 data files.
- Converting to JSON.
- Validating references and IDs.

Recommended next tool
- `convert_legacy_data` to emit `modernization-godot/data/`.
